import { tv } from 'tailwind-variants';

export const contentVariants = tv({
  base: 'bg-content z-50 min-w-72 origin-[--radix-popover-content-transform-origin] border border-secondary rounded-md px-4 pt-4 pb-2 shadow-2xl outline-none',
});

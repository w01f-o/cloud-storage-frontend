import { tv } from 'tailwind-variants';

export const contentVariants = tv({
  base: 'bg-content z-50 w-72 origin-[--radix-popover-content-transform-origin] border border-secondary rounded-md py-6 px-4 shadow-2xl outline-none',
});

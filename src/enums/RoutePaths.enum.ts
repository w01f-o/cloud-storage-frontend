export enum RoutePaths {
  HOME = "/",
  PROFILE = "/profile",
  STORAGE = "/storage",
  SHARED = "/shared",
  SETTINGS = "/settings",
  HELP = "/help",
  LOGIN = "/auth/login",
  REGISTRATION = "/auth/registration",
  FOLDER = "/folder",
}
